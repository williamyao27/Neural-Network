# Sets the target amount of testing iterations. Note that once this function is used with a number of iterations
# larger than the currently elapsed number, the model will immediately begin testing.

$scoreboard players set .test_iterations_limit global $(iterations)