# Builds a bar for the bar graph.
# Bars are hard-coded to be 2-blocks tall, 1-block thick, and extend east in length. The anchor is on the bottom-west corner.

$fill ~ ~ ~ ~$(bar_length) ~1 ~ lime_concrete