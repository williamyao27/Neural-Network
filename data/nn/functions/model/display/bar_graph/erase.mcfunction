# Erases all the bars from the model output bar graph.
# The height and width dimensions of the region to fill are hard-coded, but the length is dependent on the set value of the maximum bar length.

$fill ~ 0 ~ ~$(max_bar_length) 28 ~ air