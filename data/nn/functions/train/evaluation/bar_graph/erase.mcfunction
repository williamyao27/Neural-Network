# Erases all the bars from the accuracy report bar graph.
# The length and width dimensions of the region to fill are hard-coded, but the height is dependent on the set value of the maximum bar length.

$fill ~ ~ ~ ~100 ~$(max_bar_length) ~ air