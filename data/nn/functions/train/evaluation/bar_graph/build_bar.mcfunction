# Builds a bar for the accuracy bar graph.
# Bars are hard-coded to be 1x1 in footprint, with the height dependent on the given accuracy.
# New bars are added eastward.

$fill ~$(report) ~ ~ ~$(report) ~$(bar_length) ~ red_concrete replace air